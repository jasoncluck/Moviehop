<%= link_to 'Edit', edit_movie_path(movie), :class => "btn btn-primary" %>                        

<%= link_to 'Delete', movie_path(movie), :confirm => "Are you sure you want to destroy 	#{movie.title}?", :method => :delete, :class => "btn btn-danger" %>